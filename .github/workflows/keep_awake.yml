name: Keep App Awake

on:
  schedule:
    - cron: '0 */6 * * *' # Every 6 hours
  workflow_dispatch: # Allow manual trigger for testing

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -I https://jaswanthhanumanthu-smart-resource-allocation-ai-build-web-app.streamlit.app/
