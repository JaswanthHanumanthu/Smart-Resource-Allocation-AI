# Gemini API key: https://aistudio.google.com/apikey
# Prefer a gitignored `.env` in the project root for local dev (see `.env.example`).
# Leave the value empty to use only `.env` / environment variables.
GOOGLE_API_KEY = "AIzaSyC8Ci5ozH5z-j_dn2T5LKezVaAnobElbkI"
