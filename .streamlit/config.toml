[theme]
primaryColor = "#6366f1"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f8f9fa"
textColor = "#1e293b"
font = "sans serif"

[client]
showErrorDetails = false

[server]
headless = true
# address and port should be set via environment variables or command line for deployment (e.g., Render)
