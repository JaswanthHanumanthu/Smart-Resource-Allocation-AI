venv/
.venv/
__pycache__/
*.pyc

# API keys and local secrets (never commit)
.env
.env.*
!.env.example
.streamlit/secrets.toml
secrets.toml

# .streamlit/ may still be tracked for theme/config.toml — only secrets.toml is excluded above
