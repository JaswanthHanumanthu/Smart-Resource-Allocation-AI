# Copy this file to ".env" in the same folder as app.py (the project root).
# The real ".env" file is gitignored — never commit your API key.

GOOGLE_API_KEY=paste-your-google-ai-studio-key-here

# Optional alias (same key from Google AI Studio / Gemini):
# GEMINI_API_KEY=paste-your-google-ai-studio-key-here
